# サイクリング旅行計画の共通原則

このリポジトリは、国内サイクリング旅行を「1旅行1ブランチまたは1コピー」で管理する再利用可能な基盤です。

## 常に守ること

- 確認済み事実、推論、未確認事項、評価、提案を区別する。変動する事実には出典と確認日を残す。
- 自転車で進む行為は「自走」、電車などへ載せる移動は「輪行」と記し、この意味で別語を使わない。「サイクリング」「ツーリング」は両者を含む包括語とする。
- 候補や区間は、旅行者が現地で何をするか、どこから何を見るか、どれだけ時間を使うかまで具体化して評価する。
- 珍しさ・限定性・体験型・産業ラベルを基礎価値にせず、候補昇格は `$cycling-trip-research` の評価基準に従う。
- 希少性、知名度、歴史的重要性、写真映えなどの抽象的な魅力だけで、弱い滞在や移動を正当化しない。
- 体験価値のある移動は、時刻・待ち・輪行負担・料金・時刻表拘束・後続行動を含む通常案と比較して採用する。優先提案では時間・料金差、失う行動、今回得る体験を示す。
- 重要な時間窓、当日の主役、宿到着、翌日の選択肢、大きな追加料金に影響する差分は、暗黙にPrimeへ採用せず旅行者に選んでもらう。路線全体の知名度や代表景観を今回の根拠にしない。
- 現行案も新規案と同じ基準で再評価する。作成済みであることや調査量を採用理由にしない。
- 判断が必要なら、選択肢ごとの改善点、失うもの、未確認事項を示し、ユーザーが選べる形で返す。
- 予約、購入、電話、メッセージ送信など外部状態を変える操作は、ユーザーの明示的な依頼を受けてから行う。
- 特定旅行の日付、地域、候補、予約、current plan、Issue、調査時点の価格・営業時間を、`AGENTS.md`、`.agents/skills/`、`.codex/`、共通スキーマ、共通ツールへ固定しない。
- `issues.yaml` はエージェント側の調査・比較・計算・検証、`user_actions.yaml` は旅行者自身の予約・購入・最終判断・直前確認・準備とし、混同しない。情報不足で実行できない項目はUser Actionにしない。
- 計画更新時はUser Actionの新規・不要・Blocked解除・期限・完了を確認する。完了・不要なActionはDashboardから削除し、確定事実を関連正本へ反映する。履歴は残さない。

## 読み始める場所

1. [README.md](README.md) で正本、生成物、基本コマンドを確認する。
2. 編集対象の `schema/*.schema.json` と既存YAMLを読む。User Actionを扱う場合は `user_actions.yaml` も読む。`docs/` は生成物なので直接編集しない。
3. 作業に応じて、候補・根拠の調査は `$cycling-trip-research`、行程の作成・更新は `$cycling-itinerary-design`、成立性や体験価値の監査は `$cycling-trip-review` を使う。旅行の最新天気・予報・風・雨を更新または確認する依頼は `$travel-weather-refresh` を使う。
4. YAML変更後は `.venv/bin/python tools/validate.py`、旅行者向け文書を更新するときは `.venv/bin/python tools/render.py` を実行する。

天気は外部の可変状態であるため、保存済み予報でなくlive sourceから更新する。weather refreshだけを理由にPrime旅程を変更せず、経路変更の判断はユーザーへ返す。

## 基盤へ知見を戻すとき

個別旅行で得た知見は、複数の旅行へ適用できることを確認してから、原則、評価基準、スキーマ、検証のいずれかへ一般化する。事例固有の値と未解決事項は旅行データ側に残し、一般化した変更には対応するテストを付ける。
